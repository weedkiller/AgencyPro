@using IdeaFortune.Mailer
@using IdeaFortune.Mailer.TagHelpers
@model IdeaFortune.Core.OrganizationPeople.Emails.OrganizationPersonEmail
@{
    Layout = null;
}

<div>
    @Model.Introduction()
    @Model.PersonAdded(Model)

    @Model.Footer(SectionType.OrganizationPerson, Model.OrganizationName)

</div>