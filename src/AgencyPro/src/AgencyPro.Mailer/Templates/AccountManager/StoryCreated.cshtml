@using IdeaFortune.Mailer
@using IdeaFortune.Mailer.TagHelpers
@model IdeaFortune.Core.Stories.Emails.AccountManagerStoryEmail
@{
    Layout = null;
}

<div>
    @Model.Introduction()
    @Model.StoryCreated(Model)
    @Model.Footer(SectionType.AccountManager, Model.ProjectManagerOrganizationName)

</div>